# E2E — GNI 26-002 pursuit flow against the real tender pack (2026-07-26)

**What we did:** scaffolded a fresh pursuit repo (`~/working/gni-26002-v3`), dropped the REAL
GNI 26-002 tender pack (4 docs) into it, and ran the pursuit flow exactly as a user would:
scan → ingest → analyze → index assets → reuse analysis → draft/review → check → render.
The first pass copied a curated 13-file `_shared/` set; the drafting leg corrected that test
setup by copying the full original 29-file tree (plus the four staging duplicates already in
v3). That correction exposed the sector-direct Sibelga reference and three more product defects.
Then compared the outputs against the original, manually-run GNI bid
(`~/working/gni/01_rfp/26-002/` — the bid we actually won).

**Why this pack:** it's the strongest ground truth we have — a won bid, run entirely by hand,
so every difference between its artefacts and the method's artefacts is either a method gap or
a lesson the manual bid learned the expensive way.

## Bugs found by running the flow (all fixed same-day)

| # | Bug | Found by | Fix |
|---|---|---|---|
| 1 | The tender pack's own `1_received/_md/` escaped the manifest and image-triage lint rules — the selector "not in the pack root" silently emptied the flat pack shape. 14 untriaged scoring-table images in the RFP itself | running lint on the real pack | v1.24.0 `md_packs()` enumerates both pack shapes |
| 2 | `_shared/` root files invisible to scan — the whitelist of six kinds vs the README's own advice "add a folder rather than force a bad fit". 4 real assets (a utilities-SME CV, the team chart, a signed SoW) invisible | dropping the real GNI `_shared/` in | v1.23.0/1.24.0 scan walks all of `_shared/` |
| 3 | Converter never wrote manifest rows — the lint error had no automated path to green | first lint run after ingest | v1.24.1 `update_manifest()` upserts the row on every conversion |
| 4 | Manifest row reader `[\w./-]+` matched nothing with a space — every GNI filename has spaces, so a just-written manifest read as zero rows. Writer and reader each "worked"; together they lied | error count went 1→4 *after* backfilling the manifest | v1.24.1 `[^`]+` + spaced-filename fixture |
| 5 | Asset scan used basename substring identity. `case_studies/others/Quals.pdf` silently counted as indexed because the index contained `1_General_DataMod Quals.pdf`; the same defect hid identical filenames in different folders | copying the complete `_shared/` tree, not the curated subset | v1.27.0 exact relative-path matching; one logical asset may list editable companions, while helpers get an explicit non-evidence disposition |
| 6 | Lint and render disagreed on the same frontmatter. The planted `status: draft # lifecycle` comment was accepted by lint's first-token regex but render's shadow parser treated the whole line as the status | running the real `bid` analyse gate after R1 | v1.27.0 render delegates to `section_contract.parse_frontmatter`; inline comments are parsed once, quote-aware |
| 7 | Generated Word/PDF output was US Letter while every page budget and the RFT were A4. The script printed an "A4" check against a Letter page count | visual render QA + `pdfinfo` | v1.27.0 generated reference.docx writes A4 `w:pgSz` by default; explicit `--paper letter` remains available; buyer `--reference-doc` still wins |

Pattern worth naming: **none of these were found by reading the code.** All seven needed the
real filenames, the real folder layout, and the real sequence (convert-then-lint) to surface.

## Method-level improvements (fixed in v1.25.0)

1. **Template ↔ method mismatch.** `rfp_analysis.md.tmpl` had no clarification section, but
   method Step 6b derives clarification questions *inside* the analysis. The analyst agent had
   to invent §5b. Template now plants it.
2. **Mandatory/scored binary is strained** by scored criteria carrying minimum pass marks
   (the entire GNI Section 5: "50 of 100 to pass"). Matrix template now sanctions the
   convention: stays `D`, carries "(NN% pass mark applies)" in the requirement text.
3. **Scaffold missing two real categories.** The original GNI `_shared/` has `contracts_ref/`
   and `team_structure/`; the scaffold didn't, stranding exactly those assets at the root.
   Now planted by default.
4. **Citation order was unstated.** The ingest promised `## Page N` anchors, but pandoc
   headings aren't stable and page numbers rot on amendment. Method now says: native clause
   numbering first (`§2.1(f)` — buyer-checkable, amendment-stable), page anchors as fallback.

## New method vs original manual bid — output comparison

| Dimension | Original `01_rfp/26-002/2_analysis/` (manual) | v3 (method) | Verdict |
|---|---|---|---|
| Requirements tracking | **None anywhere** — no compliance matrix; requirements embedded in narrative change-analyses and the QA doc | 69-row matrix (46 M / 23 D), every row clause-cited, status machine-checkable, lint enforces "mandatory met" at the render gate | Method adds the spine the manual bid never had |
| Asset evidence | No `firm_assets.md`; `_shared/` folders only; "what proves what" lived in people's heads — the Sibelga date verification surfaced **months later, in delivery**, not during the bid | 16 logical assets: per-asset date, in-window verdict, permission constraint; companions/helpers explicitly disposed without fake A-ids; 6 gaps each with an owner and a route | Method catches at indexing time what the manual bid caught late or never |
| Clarification | `RFP-26-002-QA.md` — rich prose analysis, not a send-track log | 14 dimension-swept questions + 6 settled readings, each with deadline / consequence / fallback | Method's is actionable; the original's is smarter-than-average prose. **The method's "settled readings" row absorbed the original's one strength** |
| Go/no-go | No explicit artefact found | Explicit **go-if** with two named conditions (reference window, liability terms) | Method adds the gate |
| Prior-issue change analysis | **00–03 Change-Analysis.md per volume** — 25-057→26-002 deltas tracked per response volume | 20-section reuse analysis, ≈60% overall reuse, with per-volume and independently-gated-unit verdicts | Method now matches the useful granularity and adds expiry/entity/permission gates |
| Narrative depth | Deep buyer-intent interpretation ("what does 'and Reporting' mean", clause-evidence tables) | Multi-role analysis covers intent, but shallower on *this* pass | Comparable; the original had weeks, the E2E had an hour |
| §5.1.1 experience draft | 1,768 words; AIB + Sibelga; quantitatively rich but claims "100% scope completion" / schedule adherence that the held Sibelga reference does not evidence | 759 shippable words; same two examples, each in the buyer's four-angle order; unsupported outcome claims removed; `blocked-r1` on permission/referees, AIB KPIs and Sibelga member-firm/acceptance evidence | New method is safer and easier to score; original remains richer and should donate verified detail after the blockers close |
| §5.1.2 personnel draft | 4,713 words with team diagram and core CV sheets; says "five individuals" then names three, and offers some proposed-staff CVs only "on request" despite Appendix 4 | 501 shippable words; scoring split made explicit; out-of-window 2022 project removed; honestly `blocked-r1` because eight named Appendix 4 sheets and commitment/availability confirmation are absent | Original is materially more complete; method correctly refuses to confuse a summary bio with a compliant CV sheet |
| Render / package | 27-page, buyer-facing A4 Volume 2 with cover, team diagram and appendices | Five-page **internal** A4 review copy for the two drafted sections; generic formatting by design; bid render exits 2 and writes nothing | New artefact is not a replacement submission; the useful result is that the gate refuses it until R2 and evidence closure |

## What the index caught that the manual bid had no mechanism for

- **PTSB entity mismatch** (A-002/A-003): letterhead Deloitte Technology S.A. (Portugal), but
  R-017 requires the performing entity = the entity relied on. Flagged at indexing time with
  the R-014 letterhead-undertaking route. In the manual bid this lived in nobody's artefact.
- **Forest CV reference project 3 = 2022** — out of the 3-year window. The CV is otherwise
  submission-ready (already in Appendix 4 format).
- **A-005 quals pool: 9 project snapshots, zero per-project dates** — a backup pool that
  cannot back R-017 until dates are confirmed. `UNKNOWN` kept visible per the method's rule.
- **A-014 Sibelga is the strongest reference** — July–October 2024, €250k, 300 man-days,
  sole contractor, four scenarios + migration + TCO. The complete-tree pass recovered it,
  but also exposed the missing member-firm, permission, named-referee and acceptance fields.
- **A whole mandatory cluster unevidenced**: R-025–R-029 security pass/fail (AUP, access
  audits, ISO 27001/SOC 2, DLP) — no security attestation held in `_shared/` at all. The
  gaps table routes it to the CISO office with "request at kickoff, long lead time".

## E2E legs — final status

1. ~~**Run the prior-issue leg of the E2E**~~ — **DONE same day** (v1.26.0). Archived the real
   25-057 pack + our 4 response volumes into v3, converted 8/8, and ran
   `bid_reuse_analysis`: 20 sections assessed, **overall reuse ≈ 60%** (V1 pass/fail ≈ 85%,
   V2 scored ≈ 60%, V3 structure yes / price no). The analysis verified the "and Reporting"
   scope doubling at clause level (§3.3 bullet 1, §3.4, an 11th limitation + SCADA in §3.2),
   caught the §5.1.3 page cap tightening ("5 pages per question" → "5 pages for 3 questions"),
   and found the old Response Notes were non-compliant with App5 Sch.4's fixed-price-inclusive
   term — delete, not reword. Four template gaps landed as v1.26.0 (expiry register §2b,
   gated-unit verdicts, informational contract-diff sections, no-debrief outcome case).
2. ~~**Drafting + render-gate leg**~~ — **DONE** (v1.27.0). Drafted §5.1.1 and §5.1.2,
   ran manual four-lens R1, logged both as `blocked-r1`, ran strict check (0 errors / 49 real
   warnings, strict failure) and proved bid render refusal (exit 2, no artefact). Built only a
   `plain` internal review copy; Word ZIP valid, Arial 10 in styles, five A4 pages, visually
   inspected after PDF round-trip.
3. ~~**Native-clause anchors in ingest**~~ — **DONE** (v1.27.0). Word headings with native
   numbering now emit `## §x.y`; unnumbered headings keep the synthetic `§Section N` fallback.
   Verified on the real RFT: §4.5, §4.6, §4.8, §4.9 and §5.2 are now native anchors.
4. ~~**Per-volume change-analysis granularity**~~ — **DONE via leg 1**: the reuse analysis is
   inherently per-volume (V1 §4.x / V2 §5.1.x / V3), and v1.26.0's gated-unit rule takes it
   below volume level to wherever the buyer gates independently.

## Artefacts

- E2E repo: `~/working/gni-26002-v3` (kept, not cleaned — rerun target for the backlog legs)
- Internal review outputs:
  `01_pursuit/26-002/3_drafting/_render/e2e_internal_review_a4.{md,docx,pdf}`
- Shipped fixes: v1.24.0, v1.24.1, v1.25.0, v1.26.0, v1.27.0
  (plus v1.19–1.23 from the walkthrough earlier the same day)
