# {{BUCKET_LABEL}} source materials — {{CLIENT}}

**This bucket holds** {{BUCKET_SCOPE}}

Drop **originals** here, in their own folder taxonomy (create subfolders like `Data Strategy/`,
`DWH/`, `Reporting/`). Do **not** rename or edit originals.

Then run `eng-ingest-source` on each file: it converts to citable markdown under `_md/`,
handles images losslessly, and registers a manifest row in `_md/README.md`.

**Wrong bucket?** See `../README.md` for the boundary rule and the flow rules between buckets —
in particular, delivery-phase material never travels into a bid. Our own derived analysis does
not live here at all; it belongs in the phase tree.
